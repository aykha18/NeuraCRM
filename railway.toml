[build]
builder = "dockerfile"
# Force fresh build - updated 2025-01-12 - fixing frontend serving

[deploy]
startCommand = "python -m uvicorn api.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/ping"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
